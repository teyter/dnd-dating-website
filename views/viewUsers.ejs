<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Users</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <nav class="main-menu">
        <div>
            <div class="nav-text">D&D Dating</div>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/admin">Admin</a></li>
                <li><a href="/users">Users</a></li>
            </ul>
        </div>
        <div class="logout">
          <% if (locals.user) { %>
            <form method="POST" action="/logout">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <button type="submit">Logout (<%= locals.user.name %>)</button>
            </form>
          <% } else { %>
            <a href="/login" class="button">Login</a>
            <a href="/register" class="button">Register</a>
          <% } %>
        </div>
    </nav>

    <div class="content">
        <h2>User List</h2>

        <a href="/users/new" class="button">+ Create New User</a>
        <a href="/users" class="back-link">← Back to Users</a>

        <div class="profile-container" style="flex-direction: column;">
            <table>
                <tr>
                    <th>User ID</th>
                    <th>Username</th>
                    <th>Password</th>
                    <th>User Type</th>
                    <th>Actions</th>
                </tr>
                <% users.forEach(function(user) { %>
                    <tr>
                        <td><%= user.user_id %></td>
                        <td><%= user.name %></td>
                        <td><%= user.pass %></td>
                        <td><%= user.is_admin ? 'admin' : (user.user_type || 'player') %></td>
                        <td>
                            <a href="/users/<%= user.user_id %>/edit" class="button">Edit</a>
                            <form method="POST" action="/users/<%= user.user_id %>/delete" style="display:inline;">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button type="submit" class="button danger" onclick="return confirm('Are you sure you want to delete this user?');">Delete</button>
                            </form>
                        </td>
                    </tr>
                <% }); %>
            </table>
        </div>
    </div>
</body>
</html>
