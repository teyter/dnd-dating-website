<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - D&D Dating</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/homepage.css' />
</head>
<body class="home-bg">

    <nav class="main-menu home-nav">
        <div>
            <div class="nav-text">D&D Dating</div>
            <ul>
                <li><a href="/">Home</a></li>
            </ul>
        </div>
        <div class="logout">
            <% if (locals.user) { %>
                <form method="POST" action="/logout">
                    <button type="submit">Logout (<%= locals.user.name %>)</button>
                </form>
            <% } %>
        </div>
    </nav>

    <div class="content">
        <div class="home-content">
            <h1 class="home-title">Login</h1>

            <% if (error) { %>
                <p style="color: #ff4d4d; margin-bottom: 15px;"><%= error %></p>
            <% } %>

            <form method="POST" action="/login" class="auth-form">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="form-group">
                    <label>Username</label>
                    <input type="text" name="name" required />
                </div>

                <div class="form-group">
                    <label>Password</label>
                    <input type="password" name="pass" required />
                </div>

                <button type="submit" class="button button-primary">Login</button>
            </form>

            <p style="margin-top: 15px;">
                Don’t have an account?
                <a href="/register">Register here</a>
            </p>
        </div>
    </div>

</body>
</html>
