<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Monitoring</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <!-- Main Menu Sidebar -->
    <nav class="main-menu">
        <div>
            <div class="nav-text">D&D Dating</div>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/profiles/my">My Profile</a></li>
                <li><a href="/profiles/all">All Profiles</a></li>
                <li><a href="/users">Users</a></li>
                <li><a href="/admin">Admin</a></li>
            </ul>
        </div>
        <div class="logout">
            <button onclick="location.href='/logout'">Logout</button>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="content">
        <h2>Admin Monitoring</h2>

        <a href="/" class="back-link">← Back to Home</a>

        <div class="profile-container" style="flex-direction: column;">
            <h3>System uptime (shell: uptime)</h3>
            <pre><%= uptimeOut %></pre>

            <h3>App & server stats</h3>
            <ul>
                <li>App uptime: <%= Math.round(appUptimeSeconds) %> seconds</li>
                <li>System uptime: <%= Math.round(systemUptimeSeconds) %> seconds</li>
                <li>Memory (rss): <%= Math.round(memory.rss / 1024 / 1024) %> MB</li>
                <li>Memory (heapUsed): <%= Math.round(memory.heapUsed / 1024 / 1024) %> MB</li>
            </ul>

            <h3>Write a log entry</h3>
            <form method="POST" action="/admin/log">
                <div class="form-group">
                    <input name="message" placeholder="log message" style="width: 100%; max-width: 400px;" />
                </div>
                <button type="submit">Write</button>
            </form>

            <h3>Recent log (tail)</h3>
            <pre style="border:1px solid #ccc; padding:10px; white-space: pre-wrap; background: #f9f9f9;"><%= logTail %></pre>
        </div>
    </div>
</body>
</html>
